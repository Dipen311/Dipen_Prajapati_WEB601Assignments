import { TraveltypePipe } from './traveltype.pipe';

describe('TraveltypePipe', () => {
  it('create an instance', () => {
    const pipe = new TraveltypePipe();
    expect(pipe).toBeTruthy();
  });
});
